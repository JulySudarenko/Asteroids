namespace TheFifthLessonTasks.ThirdTask
{
    public class Health
    {
        private float _max;
        private float _current;

        public Health(float max, float current)
        {
            _max = max;
            _current = current;
        }
    }
}
